import { Product } from './product.model';
import { Resource } from './resource.model';

export class Coefficient {
  id: number;
  amount = 0;
  productCode = 0;
  resourceId = 0;
  // product: Product;
  // resource: Resource;
}
